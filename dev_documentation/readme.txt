use "npx fitbit" to get to the fitbit cli
use "fitbit$ bi" to build-and-install
run the fitbit simulator
the UI is made using SVG, it's similar to HTML
inkscape can create SVG files
eslint is installed using "npm install eslint --save-dev" and "npx eslint --init"
